services:
  inventory-app:
    build: .
    container_name: inventory_system
    restart: always
    # This line tells Docker to pull all variables from your .env file
    env_file: .env
    ports:
      - "3000:3000"